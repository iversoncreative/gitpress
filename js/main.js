jQuery(document).ready(function ($) {
	$(function() {
		// Appropriate classes on default menu
		$( "div.nav ul" ).addClass( "nav navbar-nav navbar-right" );
		$( "div.nav" ).removeClass( "nav navbar-nav navbar-right" );
	});
});